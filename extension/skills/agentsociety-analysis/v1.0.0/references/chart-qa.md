# Chart QA

Compatibility alias for older references. The complete checklist now lives in `references/qa-contract.md`.

## Current Role

If an older workflow still points to `chart-qa.md`, use the following sections from `references/qa-contract.md`:

1. chart-level checks
2. script-level checks
3. composite-figure checks
4. report-level checks
